# DO NOT commit real secret values. Create this with:
# kubectl create secret generic app-secrets -n ai-task-platform \
#   --from-literal=JWT_SECRET='replace_with_long_random_value'
#
# This file is included as a template only (values are placeholders, base64 of "changeme").
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: ai-task-platform
type: Opaque
data:
  JWT_SECRET: Y2hhbmdlbWU=
